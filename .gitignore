node_modules
dist
out
.DS_Store
.eslintcache
*.log*
release

# brainstorm session content + state (visual companion)
.superpowers/

# Capacitor / Android build outputs (the android/ project itself IS committed).
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/.idea/
android/captures/
.claude/
