<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--
            Android 15+ (target SDK 35) draws apps edge-to-edge by default,
            which is why the WebView was painting underneath the system
            status bar instead of below it. The StatusBar plugin's
            setOverlaysWebView call fires after JS boot — too late for
            first paint. Opt out of the enforcement at the theme level so
            the system bars inset the WebView from the start; the
            StatusBar plugin still gets to colour the bar.
        -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:statusBarColor">#000000</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Same edge-to-edge opt-out as the parent AppTheme. -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:statusBarColor">#000000</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!-- Edge-to-edge opt-out + dark status bar carry into the launch
             splash so the splash also doesn't paint under the status
             bar during the handoff to React mount. -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:statusBarColor">#15103a</item>
    </style>
</resources>
